/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/bluecircomall',
  assetPrefix: '/bluecircomall',
};

export default nextConfig;
